import ChessMachine from '../chess-machine'

const CHESS_SIZE = 8

describe('Chess Machine', () => {
  it(`should be initialize with a ${CHESS_SIZE}x${CHESS_SIZE} board`, () => {
    // const board = createBoard()
    // expect(board.length).toBe(CHESS_SIZE)

    // board.forEach((row) => {
    //   expect(row.length).toBe(CHESS_SIZE)
    // })
  })
})